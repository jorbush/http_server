[package]
name = "http-server"
version = "0.1.0"
authors = ["jorbush"]
edition = "2021"

[dependencies]
flate2 = "1.0.30"
reqwest = { version = "0.12.5", features = ["blocking"] }
assert_cmd = "2.0"

[dev-dependencies]
tempfile = "3.1"
